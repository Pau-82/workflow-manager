import { Placeholder } from '@/components/layout/placeholder';

export default function NewWorkflowPage() {
  return (
    <Placeholder
      title="Nuevo workflow"
      description="Próximamente: el formulario de alta (reusará los schemas de @org/contracts)."
    />
  );
}
