# Copiá este archivo a `.env` (mismas credenciales que docker-compose.yml).
#   cp .env.example .env
#
# Postgres corre en Docker (ver docker-compose.yml). Credenciales y puerto fijos.
DATABASE_URL="postgresql://wfm:wfm_password@localhost:5432/workflow_manager?schema=public"
