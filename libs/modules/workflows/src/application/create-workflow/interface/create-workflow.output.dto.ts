import type { WorkflowDto } from '../../mappers/interface/workflow.dto.js';

/** Output de CreateWorkflow: el workflow completo en primitivos (DTO compartido). */
export type CreateWorkflowOutput = WorkflowDto;
