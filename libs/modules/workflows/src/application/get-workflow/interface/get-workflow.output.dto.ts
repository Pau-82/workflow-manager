import type { WorkflowDto } from '../../mappers/interface/workflow.dto.js';

/** Output de GetWorkflow: el workflow completo en primitivos (DTO compartido). */
export type GetWorkflowOutput = WorkflowDto;
