import type { AlertEventDto } from '../../mappers/interface/alert-event.dto.js';

/** Output de ResolveEvent: el evento ya resuelto en primitivos (DTO compartido). */
export type ResolveEventOutput = AlertEventDto;
